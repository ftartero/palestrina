/* -------------------------------------------------------------
 *  COPIA questo file in "config.js" e compila i due valori.
 *  config.js NON va su GitHub (è in .gitignore): tiene il token.
 * ------------------------------------------------------------- */
window.CONFIG = {
  API_URL: "https://script.google.com/macros/s/XXXXXXXX/exec", // URL della Web App (finisce con /exec)
  TOKEN:   "la-tua-parola",                                     // la STESSA parola messa in SECRET nello script
};
